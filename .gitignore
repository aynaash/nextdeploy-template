# deps
node_modules/

# next
.next/
out/

# env — never commit secrets
.env
.env.*
!.env.example

# misc
*.tsbuildinfo
.DS_Store

# secrets & keys — never commit these
*.pem
*.key

# local deploy artifacts
.nextdeploy/
app.tar.gz
*-report.html
*-latest.html

# aws cli download (local only)
aws/
awscliv2.zip
